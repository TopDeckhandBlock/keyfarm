# Telegram poster (fleet/key_poster.py, fleet/dashscope_hunter.py)
TG_BOT_TOKEN=123456…ther
TG_CHAT_ID=123456789

# Encrypted-pool master key (Fernet). Generate:
#   python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
POOL_KEY=

# Your own GitHub PAT(s), comma-separated (preferred over shared pool on a node)
NODE_TOKENS=

# Optional outbound proxy
PROXY_URL=
